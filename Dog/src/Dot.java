public class Dot {
    int x;
    int y;
    Dot dot1 = new Dot(int x1,int y1);
;
    public Dot(int x, int y) {
        this.x = x;
        this.y = y;

        public distanceDot() {
            //Применив формулу d = √((хА – хВ)2 + (уА – уВ)2), получим:
int x1;
int y1;

double distanceDot =Math.sqrt((x-x1)*(x-x1)+(y-y1)*(y-y1));

            ;


        }
    }
}
